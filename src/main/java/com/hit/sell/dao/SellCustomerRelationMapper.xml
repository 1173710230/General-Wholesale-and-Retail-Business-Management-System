<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hit.sell.dao.SellCustomerRelationMapper">

    <select id="getOrderIdByCustomerId" parameterType="Integer" resultType="Integer">
        SELECT sell_order_id
        FROM sell_customer_relation
        WHERE sell_customer_relation.customer_id = #{customerId}
    </select>

    <insert id="insertRelation" parameterType="com.hit.sell.domain.SellCustomerRelation">
        INSERT INTO sell_customer_relation(scr_id, customer_id, sell_order_id)
        VALUES (#{scrId}, #{customerId}, #{sellOrderId})
    </insert>

</mapper>
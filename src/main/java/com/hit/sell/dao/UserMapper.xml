<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hit.sell.dao.UserMapper">

    <insert id="insertUser" parameterType="com.hit.sell.domain.User">
        INSERT INTO user(user_id, user_name, user_password)
        VALUES (#{userId}, #{userName}, #{userPassword})
    </insert>

    <update id="setNameByUserId" >
        UPDATE user
        SET user.user_name = #{name}
        WHERE user.user_id = #{userId}
    </update>

    <update id="setPasswordByUserId" >
        UPDATE user
        SET user.user_password = #{password}
        WHERE user.user_id = #{userId}
    </update>

    <select id="searchUserByUserId" parameterType="String" resultType="com.hit.sell.domain.User">
        SELECT user_id userId, user_name userName, user_password userPassword
        FROM user
        WHERE user.user_id = #{userId}
    </select>
</mapper>